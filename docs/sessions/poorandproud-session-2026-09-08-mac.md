# Poor and Proud Session — 2026-09-08 | Mac

## Completed Today
- Pulled first (`git fetch` + `git log --oneline main..origin/main`): 16 commits missed from the Windows sessions (2026-09-01 through 2026-09-05) — the full Calculated Metrics engine (Phase 1 + several follow-up rounds: percentile/stddev/variance aggregations, periodic metrics, a guided Metric Builder panel with live preview, day-of-week grouping, a detail-table mode, usage-aware delete) and an "Athenics" favicon/tab-title branding pass. `npx prisma generate` + `npx prisma migrate status` re-run after pulling — clean.
- **Domain rename: athenics.com → poorandproud.com.** Corey wanted to free up the "Athenics" name for something silly instead, landed on "Poor and Proud" after a long brainstorm (many alliterative/pun options discussed, not recorded here — see chat history if needed). Corey handled the account-side steps himself (registering poorandproud.com, pointing DNS, adding it in Vercel, detaching athenics.com, updating `PLAID_REDIRECT_URI`/`PLAID_WEBHOOK_URL` env vars and the Plaid dashboard's Allowed redirect URIs, redeploying — all confirmed done by him). My part was the code-side branding cleanup:
  - `app/icon.svg`: monogram letter "A" → "P" (same indigo `#6366f1` rounded-square design, unchanged otherwise).
  - `app/layout.tsx`: tab title "Athenics" → "Poor and Proud".
  - `README.md`: title `# athenics.com` → `# poorandproud.com`.
  - `AGENTS.md`: the cross-device session-log naming convention updated from `athenics-session-*` to `poorandproud-session-*` (this file itself is the first one using it) — old logs left untouched, just a note pointing at the rename.
  - Verified live via the Browser pane (not just assumed): `poorandproud.com` resolves and serves the real app; `athenics.com` now returns Vercel's own 404 (detached from the project, not a deliberately-blank page — Corey was offered that option, hasn't said yet if he wants it); `coreywogenstahl.com` doesn't resolve at all, confirming an old memory note that called *that* the live domain was simply wrong the whole time (corrected now, see Memory below). Local build/typecheck re-verified with the tab title and `/icon.svg` after the change → ✅ done
- **Corrected a wrong standing memory.** An 8-day-old note claimed the live domain was coreywogenstahl.com — untrue (verified live: `ERR_NAME_NOT_RESOLVED`). It was athenics.com the whole time. Updated `coreywogenstahl-site`, `coreywogenstahl-finance-feature`, and `financial-data-privacy` memory files' descriptions/bodies to reflect the real domain history instead of silently leaving it wrong for the next session.
- **"/" is now the real login screen, not a placeholder.** Corey asked to drop the "work in progress" placeholder and show the login directly, since the whole point of hiding it at `/quietharbor` was obscurity that a memorable public domain undermines anyway.
  - `app/page.tsx`: replaced the pixel-font placeholder with the actual login form (extracted into a shared `components/LoginForm.tsx`, reused by both this and the old path) plus a purely decorative `components/LoginBackdrop.tsx` — ghosted line/area chart, a bar cluster, and a partial progress ring, built from the exact palette `categoryColors.ts` uses for real dashboard data (indigo/orange/purple/pink/teal), so the first thing anyone sees already looks like the app underneath. Verified in light, dark, and creamsicle — legible and tasteful in all three (the login card itself has a solid/blurred background so this never depends on staying subtle).
  - Added a tagline ("It's not a budget, it's a personality.") as the "fun flare" ask — easy to change or drop if it's not landing.
  - `app/quietharbor/page.tsx`: retired to a plain redirect to `/`, for anyone with the old path bookmarked — not deleted outright.
  - Caught and fixed one gap before it shipped: proxy.ts exempts `/` from the auth check unconditionally (it has to, for a logged-out visit to work), so without a check *in* the page itself, an already-logged-in visitor would see the login form again instead of their dashboards. `app/page.tsx` now checks `requireAdminSession()` first and redirects to `/dashboards` if already logged in.
  - `proxy.ts` comments updated to stop describing `/` as a placeholder and `/quietharbor` as "the only place the real login form exists" — both were now false.
- **Login rate-limiting, added proactively.** Corey asked directly whether the login needed hardening now that it's public instead of hidden — it did: username/password comparison was already constant-time (`timingSafeEqual` over SHA-256 hashes, fine as-is), but there was no brute-force protection *at all*, which mattered a lot less while the login path itself was the secret. New `LoginAttempt` model (one row per client IP — `lib/loginThrottle.ts`) tracks failed attempts in Postgres rather than in-memory (deliberately — Vercel serverless functions don't share memory across instances, so an in-memory counter would barely slow anything down): 5 failures in 15 minutes trips a 15-minute lockout for that IP, checked *before* the submitted credentials are even looked at (so a locked-out attacker can't keep using attempts as a timing oracle), cleared entirely on a successful login. `app/api/auth/login/route.ts` wired in; error redirect target moved from `/quietharbor?error=1` to `/?error=1` to match the new front door, plus a new `?error=locked&minutes=N` case.
  - Verified for real, not just read through: 6 scripted `curl` POSTs from a fake IP (`X-Forwarded-For`) against the actual local server hit the real (shared) Postgres — attempts 1–5 each got `?error=1`, attempt 6 correctly got `?error=locked&minutes=15`, and a 7th confirmed the lockout holds. Also clicked through the wrong-password and lockout UI states directly (`?error=1`, `?error=locked&minutes=12`) to confirm the copy renders right. Deleted every test `LoginAttempt` row (including one stray real-browser attempt from `::1`) afterward — nothing test-related left in the shared DB. → ✅ done

- **Follow-up tweaks to the login screen**, same session: Corey asked to make the backdrop charts read as bad news (the line now crashes downward, bars hang below a zero line, the meter reads nearly maxed-out — all in red tones instead of the original mixed palette) and to drop the "Poor and Proud" heading/tagline entirely, leaving just the two fields and the button — the joke now lives entirely in the backdrop rather than being spelled out in text. Also asked directly about injection attacks via the username/password fields: checked and confirmed not applicable here — the login route compares submitted credentials against env vars via `timingSafeEqual`, never builds a SQL query or any other command from them at all, so there's no injection surface the way there would be with a raw `WHERE username = '${input}'`-style query. Confirmed no raw Prisma `$queryRaw`/`$executeRaw` usage anywhere in the app either (everything goes through Prisma's parameterized query builder), and the only `dangerouslySetInnerHTML` in the codebase is a static, hardcoded theme-init script with no user input in it — not reflecting the login fields or anything else user-supplied. Reverified visually in all three themes after the changes → ✅ done

- **Backdrop redesigned again, once more down to one chart.** Corey didn't like the donut/meter and asked to keep only the line chart — but flagged the result as "a bit bland" once the bars and meter were gone. Rebuilt it as one full-bleed layered chart instead of a small corner accent: an SVG `linearGradient` area fill under the crash line, a dashed "budget" reference line the real line visibly crashes through (a little narrative rather than just an abstract line), a fainter second line behind it for depth, faint horizontal grid lines for a dashboard feel, and a soft glow filter (`feGaussianBlur` + `feMerge`) on the primary line and its vertices. Reverified in all three themes again → ✅ done

- **Change-password functionality, plus a 2FA scoping detour.** Corey asked for password-change and floated 2FA ("just cool to have"). Confirmed scope with him first: build both together, no backup codes for 2FA lockout (direct DB access as the recovery path instead). Started building TOTP two-factor (lib/totp.ts RFC 6238 implementation, a two-step login flow with a short-lived pending-2FA cookie, QR setup via a new `qrcode` dependency) — mid-install, `npm audit` surfaced something unrelated but serious enough to stop and flag immediately: **Next.js 16.0.0–16.3.2 (this project is on 16.3.0) has a critical unauthenticated RCE** (two CVEs — one Windows-hosted, one via AVIF image optimization). Reported it to Corey right away and spun it off as a separate background task (not fixed today — out of scope for this thread, and mixing an unrelated critical framework patch into this change would make both harder to review). Corey then said to drop 2FA entirely and just do password change — `qrcode`/`@types/qrcode` uninstalled again, package.json/lock back to clean.
  - **Password moved out of the `ADMIN_PASSWORD` env var into Postgres** — a running instance can't rewrite its own env vars, so there was no way to actually implement "change password" while the password lived there. New `AdminCredential` model (one singleton row), `lib/adminCredentials.ts`: scrypt password hashing (salt:hash, not the old sha256+timingSafeEqual trick — that was fine for constant-time *comparison* but not real password *storage*), lazy-seeds the row from `ADMIN_USERNAME`/`ADMIN_PASSWORD` the first time anything checks credentials post-deploy, never touches the env var again after that.
  - `app/api/auth/login/route.ts` now checks the DB via `verifyLoginCredentials` instead of comparing against env vars directly.
  - New Settings → Account section (`components/ChangePasswordForm.tsx` + `POST /api/settings/change-password`) — requires the current password, not just an active session, same as any real "change password" flow.
  - **New recovery script**, `scripts/reset-admin-password.mjs` — since the password now lives in the DB (not an env var you could just edit in Vercel), losing it entirely needed a real way back in given Corey's own "I'll fix it via direct DB access" call. Resets the singleton row's password hash directly against the database.
  - Cleaned up leftover `/quietharbor` double-hops while in the area — `logout` and every protected page's `redirect("/quietharbor")` now redirect straight to `/` (the real login screen since earlier today), since `/quietharbor` is just a redirect stub now.
  - **Verified carefully, without ever touching or exposing the real password**: the scrypt hash/verify/change-password round trip in complete isolation (no DB) via a throwaway script — correct-password accepts, wrong-password/empty/corrupt-value all reject, salts differ per hash, old password rejected and new one accepted after a simulated change. Separately verified the real seeding + rejection path against the actual shared DB using a *deliberately wrong* test password over curl (safe — never touches or requires the real one): confirmed the `AdminCredential` singleton seeded correctly from the real env vars (username matched, hash correctly formatted) and the wrong-password attempt was correctly rejected. Did **not** test the "correct password succeeds" or "change password" happy paths live, on purpose — either would have required typing Corey's real production password into a command, or deliberately overwriting it with a test value; recommend he does that one test himself once deployed. → ✅ done (password change); 🚫 2FA not built, per his own call

- **Patched the critical Next.js RCE from earlier today**, immediately once Corey asked about it directly ("there is bank accounts linked here!!!"). Looked up both CVEs' actual advisory text rather than trust the `npm audit` one-liner: GHSA-p293-qw3h-jr36 (path traversal RCE) explicitly requires **Windows-hosted** servers — this deploys on Vercel (Linux), not applicable; GHSA-2xp9-vwfh-vxw4 (AVIF image-optimization RCE via `sharp`/`libheif`) requires using `next/image`'s optimization API with AVIF files — grepped the codebase, this app never uses `next/image` anywhere and has no `images` config, also not applicable. Patched anyway as free defense-in-depth (a patch-version bump, 16.3.0 → 16.3.4, not a risky major upgrade) rather than leave a known-critical CVE sitting there just because it's reasoned to be unreachable today.
  - `npm audit fix` also picked up a `js-yaml` DoS fix for free (transitive via `eslint`, dev-tool-only, never touches production).
  - Left one dev-only DoS vector unpatched on purpose: `deepmerge-ts` (via `@prisma/config`/the `prisma` CLI) needs a breaking downgrade to `prisma@6.12.0` to fix, and the vulnerable code path is the CLI's own config-loading, not anything reachable through the running server that actually handles requests/bank data — not worth losing 7 versions of Prisma over.
  - Fixed npm's auto-added `^` caret on `next`/`eslint-config-next` back to exact pins, matching how this fast-moving, non-standard Next.js version was already pinned before.
  - Verified for real: full `next build` clean, then a live smoke test — login page renders, a wrong-password attempt still correctly rejects, and an unauthenticated request to `/dashboards` still gets served the login page's content (not real data) via the proxy rewrite, confirming the auth gate itself wasn't weakened by the version bump. → ✅ done

- **Second login account.** Corey wanted a second account for cgcamryn — floated per-user dashboards + a combined household view first, but scoped that back down himself once I laid out the real size of it (this schema has zero multi-user concept anywhere — no owner on FinanceAccount/Dashboard/CalculatedMetric, sessions don't carry an identity — a real per-user model, not a quick add). Landed on the simpler version: a second real login, identical full access to everything, no per-user separation.
  - Gave a direct opinion on his "combine on the client side?" question before any of this: recommended against it — client-side combination would need raw decrypted transactions from both people sent to the browser (worse than today's server-side-aggregated model), and would silently produce *wrong* numbers for anything non-additive (median/percentile/stddev, which the metrics engine now supports) — averaging two pre-computed medians isn't the same as the true combined median. Recommended server-side combination instead, which turns out to need no new mechanism at all: widgets already filter by `accountIds` or leave it open; a combined dashboard is just one that doesn't scope to one person's accounts. Not built (per his own scoping decision), but worth remembering for when per-user dashboards do get built for real.
  - `AdminCredential` converted from a hardcoded singleton (one row, id always `"singleton"`) to a real small table (`id` now a real `cuid()`, `username` now `@unique`) — migration written by hand (`prisma migrate dev` refused non-interactively over the unique-constraint warning, even with `--create-only`; same non-interactive-environment issue this project has hit before) and applied via `prisma migrate deploy`.
  - `lib/adminCredentials.ts` reworked: `verifyLoginCredentials` looks up by username now instead of a fixed row, with a dummy scrypt hash run on a miss so a nonexistent username doesn't respond measurably faster than a real one (timing-based username enumeration). `changePassword` takes a username, no longer always "the one account."
  - Session tokens now carry `username` (`lib/session.ts`'s `createSessionToken`/new `getSessionUsername`, `lib/auth.ts`'s new `getCurrentUsername`) purely so Settings' change-password form knows *whose* password it's changing — not a permission check, since every account still has identical access. `app/api/settings/change-password/route.ts` handles a pre-this-change session token gracefully (no `username` claim yet) by asking for a fresh login rather than erroring.
  - New `scripts/add-account.mjs` (generates a real random password if none given) and reworked `scripts/reset-admin-password.mjs` to take a username, now that "the one account" doesn't uniquely identify a row anymore.
  - **Turned down setting the password to "1234"** as asked, twice, with the reasoning restated each time (rate limiting doesn't help if the very first guess is correct, and "1234" is the first or second guess any bot tries) — generated a real 96-bit random password instead via the new script.
  - **Verified as thoroughly as the earlier password-change work, but with real freedom to test the full happy path this time** — since this is a fresh account I just created, not the primary real admin's own secret. Wrong password rejected; correct (generated) password succeeds and its session cookie's JWT payload decodes to exactly `{role:"admin", username:"cgcamryn", ...}`; full change-password round trip (old password rejected after change, new one works, wrong current-password rejected); then reset back to the originally-generated password via the recovery script so the final state matches what Corey was actually given. Confirmed the original `diorgarments` account still rejects a wrong password correctly and both accounts coexist in the table afterward. → ✅ done

- **Dropped the favicon, tab title now literally "poorandproud".** Corey wanted the "P" monogram icon gone entirely and the tab text to read `poorandproud` (lowercase, one word) instead of "Poor and Proud". Deleted `app/icon.svg`; `app/layout.tsx`'s metadata title updated. Also cleaned up `proxy.ts`'s matcher/comment, which had a specific exemption carved out just to serve that icon file to logged-out visitors — removed now that there's no icon to protect (left a note for if one's ever added back). Verified `/icon.svg` no longer resolves to real image data (it 200s with the login page's own rewritten HTML now, same as any other nonexistent path under the auth proxy — not a real favicon route anymore) and the tab title reads exactly `poorandproud`. → ✅ done

## Code Changes
- Files modified: `app/icon.svg`, `app/layout.tsx`, `app/page.tsx`, `app/quietharbor/page.tsx`, `app/api/auth/login/route.ts`, `app/api/auth/logout/route.ts`, `proxy.ts`, `prisma/schema.prisma`, `README.md`, `AGENTS.md`, `components/LoginBackdrop.tsx` (redesigned twice same session), `components/LoginForm.tsx`, `app/(site)/settings/page.tsx`, `app/(site)/dashboards/page.tsx`, `app/(site)/dashboards/[id]/page.tsx`, `app/(site)/data-hub/page.tsx`
- New files (password change): `lib/adminCredentials.ts`, `components/ChangePasswordForm.tsx`, `app/api/settings/change-password/route.ts`, `scripts/reset-admin-password.mjs`, migration `20260909010353_add_admin_credential`
- Also modified (Next.js patch): `package.json`, `package-lock.json` — `next`/`eslint-config-next` 16.3.0 → 16.3.4
- Also modified (second account): `lib/adminCredentials.ts`, `lib/session.ts`, `lib/auth.ts`, `app/api/auth/login/route.ts`, `app/api/settings/change-password/route.ts`, `prisma/schema.prisma`, `scripts/reset-admin-password.mjs`
- New files (second account): `prisma/migrations/20260909014056_allow_multiple_accounts/`, `scripts/add-account.mjs`
- Also modified (favicon/title): `app/layout.tsx`, `proxy.ts`; deleted `app/icon.svg`
- New files: `components/LoginForm.tsx`, `components/LoginBackdrop.tsx`, `lib/loginThrottle.ts`, this session log
- Schema/migration: yes — new `LoginAttempt` model, migration `20260909001622_add_login_attempt_throttle`, applied directly to the live (single, shared) DB. Purely additive (new table only).
- Git commit(s): not yet — see Uncommitted Work

## Uncommitted Work
- Everything in "Code Changes" above is made and locally verified (`tsc --noEmit`, `eslint .`, full `next build` all clean; login throttle verified end-to-end against the real DB, then cleaned up) but **not yet committed** — waiting on Corey to confirm before pushing/merging to `main`, per this session's own default (ask before pushing, even though he's said yes quickly each time recently).

## What's Next
- Corey hasn't said whether he wants `athenics.com` to show a deliberately blank page instead of Vercel's raw 404 — if he does, that needs the domain re-attached to the Vercel project plus a small host-based check in `proxy.ts` (not built; straightforward if wanted).
- `LoginAttempt` rows never get cleaned up for IPs that just stop trying — fine at this scale (one admin account, low attack volume expected), but worth a periodic cleanup if the table ever grows enough to matter.
- Everything from the 2026-09-05 Windows session's "What's Next" is still open and untouched by today: Metric Builder Phase 2 candidates (goals/streaks/rates/composites/forecasting/outliers), the still-unfixed widget-editor drawer mobile layout, and `RecurringGroup` still not wired into any metric/dashboard (and still never scanned against real data — that's still Corey's call, not run today either).
- Blockers/notes: none. `tsc --noEmit`, `eslint .`, and a full `next build` ran clean after every change today.
