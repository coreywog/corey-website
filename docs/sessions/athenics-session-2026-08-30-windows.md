# Athenics Session — 2026-08-30 | Windows

## Completed Today
- Multi-series widgets: per-series quick buttons ("All spending" / "All income" / "Net") next to the category picker — the only way to plot a whole-widget income or net line, since actual merchant categories never include income → ✅ done
- "Date focus buttons" section renamed to "Buttons"; new "Series" toggle-pill row a viewer can turn on to hide/show individual lines/areas/bars on the live tile (client-side only, per-viewer, never touches saved config) → ✅ done
- **New: cumulative (running total) lines.** A "Running total instead of per-day/per-month" checkbox — available for a single-metric widget and per-series in multi-series mode, whenever groupBy is Day/Month — with a basis choice: "Start at 0 for this range" (resets at the window's first bucket) or "Continue from before this range" (carries in every matching transaction before the window as a starting offset) → ✅ done
- **Fixed three real chart-rendering bugs**, all found via a local repro harness (rendered `<Widget>` directly with hand-built data, briefly bypassing the auth proxy for one throwaway dev route each time, reverted after) rather than guessed at:
  - A recharts render-time exception (its "nice tick" math misbehaving at certain tickCount/domain combos) had no error boundary around it, so it could take out more than the one tile — added `ChartErrorBoundary` in Widget.tsx.
  - Grid lines were rendering but at 15% opacity of the default gray — essentially invisible against the creamsicle theme's cream background. Bumped to `#888888` at 30%.
  - Charts went fully blank at small tile sizes: fixed per-chart overhead (margins + the X axis's own reserved height for rotated labels, ~55-65px) was exceeding the whole box, collapsing the plot area's clipPath to `height="0"`. Added `minWidth`/`minHeight` to every `ResponsiveContainer`; below that floor it renders cropped (via the tile's own `overflow-hidden`) instead of blank. Also lowered `DashboardGrid`'s new tile-size floor from 3x3 down to 2x2 grid units so six tiles still fit per row.
  - A bar looked "off-center" whenever an axis's ticks were hidden (`showXTicks`/`showYTicks: false`): the axis's own reserved width/height (YAxis `width={56}`, bar/stackedBar's rotated-label XAxis `height={40}`) stayed fixed even with nothing drawn there, skewing everything else toward the opposite side. Added `yAxisWidth`/`xAxisHeightRotated` to collapse that reservation to 4px when hidden — measured 64px/8px gaps going to a symmetric ~27px/23px on the exact reported case → ✅ done, verified with actual DOM/pixel measurements each time, not just re-described
- **Custom widget titles can now auto-update their date text.** A title can include the literal token `{date}` anywhere (`TITLE_DATE_TOKEN`, exported from Widget.tsx) and it resolves live to the same `describeDateRangeSelection` text the auto-generated title already used — "Food Budget — {date}" reads as "Food Budget — July" today, "Food Budget — August" once it turns. Editor's Title field explains it, previews the current resolution, and has an Insert button → ✅ done
- **Dashboard nav restructuring.** Tabs (with reordering — new, via per-row ↑/↓), the publish toggle, and delete-dashboard all moved out of a horizontal header bar on the dashboard page and into the sidebar: clicking a dashboard pops its tabs open underneath it (`DashboardNavItem`), with an "Add tab" row styled like the sidebar's own "Create Dashboard" link. Switching tabs is a real navigation now (`?tab=<id>`) instead of local component state, since the sidebar and the dashboard page are separate parts of the tree — the dashboard page now only computes the *active* tab's widgets instead of every tab up front. `DashboardTabs.tsx` is gone; replaced by `DashboardNavItem.tsx` (sidebar), `DashboardNameHeading.tsx` (the one thing left on the main page — click-to-rename), and `types.ts` (`CalculatedMetricOption`, the one thing `DashboardGrid`/`WidgetEditorPanel` still needed from the old file) → ✅ done, verified visually via a throwaway mock (real component's `usePathname()`-based active state can't be faked without a real dashboard row to navigate to, so the JSX/CSS was checked visually and the routing logic by reading the code)
- **Nav follow-ups, same day, after Corey's first look at the above:**
  - Tried a *second* column popped out to the right of the sidebar for the expanded dashboard's panel (a React portal into a `#dashboard-panel-slot` div added to the layout) — Corey said it looked bad (a lot of empty space between the two columns). Reverted to nesting under the dashboard's own link after all, but widened the sidebar (`w-48` → `w-64`) so it isn't cramped, and moved `DashboardNameHeading`'s rename UI into the panel itself (deleting that component) since the standalone page heading was now redundant.
  - Tab reordering switched from ↑/↓ buttons to native HTML5 drag-and-drop (`draggable`/`dragstart`/`dragover`/`drop`, no library) per request — `reorderTabs` reassigns every tab's `order` to its new array position in one pass (a drag can move a tab several places at once, not just swap two adjacent ones).
  - **Real tab-switch performance fix.** The dashboard page was unconditionally re-running four editor-only queries (accounts, category options, decrypted merchant names, saved calculated metrics) on *every* tab click, even on a published (view-only, no editor) dashboard — the merchant list alone decrypts every spending transaction's description app-wide. Moved to a new `/api/dashboards/[id]/editor-context` route, fetched by `DashboardGrid` only the first time the widget editor actually opens, cached at module scope (not component state, since `DashboardGrid` remounts on every tab switch) so it's fetched at most once per page load.
  - All verified the same way: real component with fake props via a throwaway `/dev/nav-test` route (temporarily bypassed in `proxy.ts`, fully reverted before each commit) — including confirming a simulated drag actually fires `reorderTabs` and surfaces a network error correctly against a nonexistent fake dashboard id.

## Code Changes
- Files modified: `components/dashboards/Widget.tsx`, `components/dashboards/WidgetEditorPanel.tsx`, `components/dashboards/DashboardGrid.tsx`, `components/nav/DashboardNavList.tsx`, `components/nav/DashboardNavItem.tsx`, `lib/dashboardConfig.ts`, `lib/dashboardQuery.ts`, `app/(site)/layout.tsx`, `app/(site)/dashboards/[id]/page.tsx`, `app/api/dashboards/[id]/route.ts`, `app/api/dashboards/[id]/tabs/[tabId]/route.ts`
- New files created: `components/nav/DashboardNavItem.tsx`, `components/dashboards/types.ts`, `app/api/dashboards/[id]/editor-context/route.ts`
- Deleted: `components/dashboards/DashboardTabs.tsx`, `components/nav/DashboardNavLink.tsx`, `components/dashboards/DashboardNameHeading.tsx` (all superseded — see above)
- No schema/migration changes — `cumulative`/`cumulativeBasis`/`showSeriesToggles` are all-optional `WidgetConfig` JSON fields (`Dashboard`/`DashboardWidget.config` is already a JSON column); tab reordering reuses the `DashboardTab.order` column that already existed, just newly exposed via PATCH and actually driven by the UI now
- Git commits (chronological, all pushed to `origin/main`): `52dae9f`, `d382215`, `227debd`, `026ca00`, `7970c64`, `0c37aaf`, `c218d3b`, `e84b7a7`, `b91978d`, `3bd6723`, `c6c5fcb`, `2275b11`

## Uncommitted Work
- None — working tree clean aside from the pre-existing untracked dirs outside this project's scope (`app/(site)/dev/records/`, `components/claw/`, `components/records/`, `components/scene/`, `lib/claw/`, `lib/menu/`, `lib/random.ts`, `lib/records/`, `lib/scene/`)

## What's Next
- Cumulative buckets: a day/month with zero matching transactions only gets folded into the running total as an implicit $0 (the running line stays flat through it); it still won't appear at all if *no* series/metric has any data that period, since the x-axis itself is built from whichever buckets have at least one row somewhere. Not an issue for the common case (an active account rarely has a truly empty month) — worth a real fix only if it turns out to bite.
- Tab reordering's two-PATCH swap approach (reassign by array position) only ever touches an adjacent pair — there's no way to jump a tab several spots in one action, only step by step. Fine for the handful of tabs a personal dashboard actually has; would need a real bulk-reorder endpoint if that ever changes.
- Not manually verified against real login/data in the browser this session for anything after the first three cumulative-line items — no persistent auth session available here (a password-manager-style credential handoff was tried once for the Y-tick crash investigation and correctly refused; every fix after that was verified via a throwaway local repro route instead, auth-bypassed in `proxy.ts` only for that one route and reverted immediately after each check — never committed). Recommend clicking through the sidebar nav changes and the small-tile chart fixes for real on next login, since those got the most code churn today.
- Blockers/notes: none. `tsc --noEmit`, `eslint .`, and a full `next build` were run clean after every change today, including after every cleanup of a throwaway dev route.
