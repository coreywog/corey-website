import { ComingSoon } from "@/components/ui/ComingSoon";

export default function AdminPage() {
  return (
    <ComingSoon
      title="Admin"
      description="Private workout entry form — password protected, not linked from the site nav."
    />
  );
}
